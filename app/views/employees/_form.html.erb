<%= simple_form_for(employee, url: { controller: "employees", action: action}) do |f| %>

	<div class="form-inputs">
		<%= f.input :name %>
		<%= f.input :email %>
		<%= f.input :cpf %>
		<%= f.input :rg %>
	</div>

	<div class="form-actions">
		<%= f.button :submit %>
	</div>
<% end %>
