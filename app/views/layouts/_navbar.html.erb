<nav class="navbar navbar-default navbar-inverse" role="navigation">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="navbar-collapse"> <!-- Add collapse in class -->
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><b>Entrar</b>
            <span class="caret"></span>
          </a>
          <ul id="login-dp" class="dropdown-menu">
            <li>
              <div class="row">
                <div class="col-sm-12">
<!--
                    Login via
                    <div class="social-buttons">
                      <a href="#" class="btn btn-fb"><i class="fa fa-facebook"></i> Facebook</a>
                      <a href="#" class="btn btn-tw"><i class="fa fa-twitter"></i> Twitter</a>
                    </div>
                                    or
-->
                  <%= form_for :user_session, url: :user_session, html: { id: 'login-nav' } do |f| %>

                    <div class="form-group">
                      <%= f.label :email, class: 'sr-only' %>
                      <%= f.text_field :email, class: 'form-control', placeholder: 'e-mail' %>
                    </div>

                    <div class="form-group">
                      <div class="form-group">
                        <%= f.label :password, class: 'sr-only' %>
                        <%= f.password_field :email, class: 'form-control', placeholder: 'senha' %>
                      </div>
                      <div class="help-block text-right">
                        <%= link_to 'Esqueceu a senha?', new_user_password_path %>
                      </div>
                    </div>

                    <div class="form-group">
                      <%= f.submit 'Entrar', class: 'btn btn-primary btn-block' %>
                    </div>

                    <div class="checkbox">
                      <label><input type="checkbox"> Lembrar-me</label>
                    </div>
                  <% end %>
                </div>
                <div class="bottom text-center">
                  Não tem uma conta? <%= link_to 'Inscreva-se', new_user_registration_path %>
                </div>
              </div>
            </li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
